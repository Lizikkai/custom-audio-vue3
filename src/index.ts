/**
 * 项目入口文件
 * 用于导出项目的主要组件、工具函数等
 */

// 导出组件
// export { default as ExampleComponent } from './components/ExampleComponent.vue'

// 导出工具函数
// export * from './utils'

// 导出类型定义
// export type * from './types'

// 默认导出
export default {
  // 在这里添加需要默认导出的内容
}
